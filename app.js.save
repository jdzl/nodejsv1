.render('index.html');// servidor.js
var express = require('express');
var app = express();
var port = 8080;

app.get('/', function(req, res) {
  return res.send('Hola, mundo!');
});

app.use(express["static"](__dirname + '/public'));

console.log('Escuchando en el puerto ' + port);
app.listen(port);
